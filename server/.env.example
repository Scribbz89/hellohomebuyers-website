# Copy to .env for local testing. On Render, set these as real
# environment variables in the service's dashboard instead of committing them.

RESEND_API_KEY=re_your_resend_api_key
NOTIFY_EMAIL=lewis@hellohomebuyers.net
FROM_EMAIL=onboarding@resend.dev
ALLOWED_ORIGIN=https://hellohomebuyers-website.onrender.com
PORT=3000
